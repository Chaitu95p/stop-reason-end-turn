# src/api/ — HTTP handler layer

Any file under this directory triggers `.claude/rules/api-conventions.md`
to load. Test the path-scoping by editing a file here and asking Claude
about the error envelope — it should quote RFC 7807 without prompting.
