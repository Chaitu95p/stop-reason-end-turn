[project]
name = "stop-reason-end-turn"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.115.1",
]

[tool.uv.workspace]
members = ["modules/module-*", "exercises/exercise-*"]
exclude = ["exercises/exercise-2-team-workflow"]
